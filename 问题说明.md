# Codex 代理问题诊断与解决方案

## 🔍 问题根源

LiteLLM 无法正确获取环境变量中的 API Key，导致 `Authentication Fails (governor)` 错误。

## ✅ 推荐方案

### 方案 1：使用官方 OpenAI Codex（最简单，推荐）

你已经配置好了官方 OpenAI Codex，直接使用：

```powershell
codex app
```

这个方案已经可以工作了！

---

### 方案 2：修复并使用国产模型代理

如果坚持要用国产模型，我们来诊断一下：

#### 第一步：测试 DeepSeek API 是否正常

双击运行：
```
D:\maozhua\Codex\简单测试.ps1
```

如果这个测试成功，说明 API Key 是好的。

#### 第二步：重新启动服务

先关闭所有相关窗口，然后运行：
```
D:\maozhua\Codex\启动.ps1
```

然后再运行桌面上的 `Codex启动.bat`。

---

## 📁 已创建的文件

| 文件 | 用途 |
|------|------|
| `简单测试.ps1` | 直接测试 DeepSeek API |
| `启动.ps1` | PowerShell 版本的启动脚本（更可靠） |
| `重启服务.bat` | 旧的重启脚本 |

## 💡 我的建议

**直接使用官方 OpenAI Codex！**

你之前已经配置好了，它不需要本地代理，可以直接工作：

```powershell
codex app
```

这是最简单、最可靠的方案。
