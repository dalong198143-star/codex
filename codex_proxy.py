"""
DEPRECATED — single-file version, replaced by modular src/ package.

See:
  src/server.py       — Flask server + entry point
  src/converter.py    — Responses API → Chat Completions protocol converter
  src/sse_builder.py  — SSE event construction (streaming + non-streaming)
  src/config.py        — Configuration constants with env var overrides
"""
